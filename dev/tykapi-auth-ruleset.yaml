extends: 'spectral:oas'

rules:
  check-for-jwt-auth:
    description: APIs does not have JWT enabled
    given: "$.api_definition"
    severity: error
    then:
      field: 'enable_jwt'
      function: pattern
      functionOptions:
        match: '^(?!\s*$).+'